{
  "name": "sequelize-tutorial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "make-model": "npx seqelixe-cli model:generate --name user -- attributes email:string",
    "migrate" : "npx sequelize-cli db:migrate",
    "make-seed" : "npx sequelize-cli seed:generate --name demo-user",
    "seed" : "npx sequelize-cli db:seed:all",
    "down" : "npx sequelize-cli db:migrate:undo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^2.3.3",
    "sequelize": "^6.19.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.4.1"
  }
}
